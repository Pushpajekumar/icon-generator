import React from "react";
import GenerateForm from "../_components/generate/generate-form";

const page = () => {
  return (
    <main className="mt-20">
      <GenerateForm />
    </main>
  );
};

export default page;
